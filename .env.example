# Optional: StreamVibe works with a curated demo catalog without credentials.
# Add either a TMDB API key or read-access token to enable live data.
VITE_TMDB_API_KEY=
VITE_TMDB_BEARER=

# Optional HTTPS endpoint that accepts the support form as JSON.
VITE_SUPPORT_ENDPOINT=
